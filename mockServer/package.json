{
    "name": "json-server-demo",
    "main": "server.js",
    "scripts": {
        "default": "json-server --c config.json -w db.json ",
        "demo": "json-server -w ./demo/db.js",
        "starts": "node server.js"
    },
    "devDependencies": {
        "json-server": "^0.17.0",
        "nodemon": "^2.0.20"
    },
    "dependencies": {
        "glob": "^8.0.3"
    }
}
